# Mnémlet v0.2 Push-Readiness Plan (P0 bundle)

**Goal:** Bring the public-facing artefacts (README, AGENTS.md, install
command) in line with the actual v0.2.0 code, so the first ever `git
push` to the public GitHub repository does not ship factual errors or a
broken Quickstart.

**Scope:** Documentation and metadata only. No source code or test
changes. No PyPI publish. No git push. No website redesign (that belongs
in the P1 round).

**Tech Stack:** Markdown, plain text, git. No build steps.

---

## Required Constraints

- Work in the main repo: `/home/christoph/mnemlet`. No worktree needed.
- Tests must remain green via `.venv/bin/pytest`. `uv` is not installed.
- Do not push.
- Do not publish to PyPI in this plan. PyPI is a separate plan.
- Do not touch productive OpenWebUI or live Mnémlet services.
- All claims in user-facing files must be verifiable from the current
  source tree at commit `baead96 release: v0.2.0` or later.
- Pause between tasks: implementer reports task completion + evidence,
  then waits for go-ahead before starting the next task.

---

## Source of Truth (extracted from current code at `baead96`)

### MCP tools — 14 (not 8)

From `src/mnemlet/server/mcp_server.py`:

1. `mnemlet_ingest`
2. `mnemlet_recall`
3. `mnemlet_context`            *(v0.2, new)*
4. `mnemlet_explain`            *(v0.2, new)*
5. `mnemlet_remember`           *(v0.2, new)*
6. `mnemlet_forget`             *(v0.2, new)*
7. `mnemlet_replace`            *(v0.2, new)*
8. `mnemlet_confirm`            *(v0.2, new)*
9. `mnemlet_search`
10. `mnemlet_status`
11. `mnemlet_namespaces`
12. `mnemlet_update`
13. `mnemlet_decay_config`
14. `mnemlet_export`

The six v0.2 additions all come from the Memory Intelligence Core
(context packs, explain provenance, status-aware review commands).

### REST endpoints — 16 + 1 MCP SSE

From `src/mnemlet/server/routes/*.py`. Router prefixes must be verified
during Task 1 by reading each router file; the path list below is the
*relative* path that appears on `@router.(get|post|put)`:

- `GET  /health`
- `GET  /status`
- `GET  /vault`
- `POST /ingest`
- `POST /recall`
- `POST /context`               *(v0.2, new — Context Pack API)*
- `POST /remember`              *(v0.2, new — Intelligence ingest)*
- `POST /forget/{memory_id}`    *(v0.2, new — review)*
- `POST /replace/{memory_id}`   *(v0.2, new — review)*
- `POST /confirm/{memory_id}`   *(v0.2, new — review)*
- `GET  /explain/{memory_id}`   *(v0.2, new — provenance)*
- `POST /decay/run`
- `GET  /namespaces/{namespace}/decay`
- `PUT  /namespaces/{namespace}/decay`
- `POST /start` (sleep router)
- `POST /stop`  (sleep router)
- `SSE  /mcp`

The current README API table lists 11 entries; 6 v0.2 endpoints are
missing and the table also references `/api/v1/sleep/status` which no
longer appears in `sleep.py` (must be confirmed during Task 1).

---

## Decision needed before Task 1

The current README Quickstart says `pip install mnemlet`. v0.2.0 is not
on PyPI, and we explicitly chose not to publish in this plan. Pick one:

- **Option A — git install only.** Change the Quickstart to
  `pip install git+https://github.com/christoph/mnemlet.git@v0.2.0`.
  Add one sentence: "PyPI release follows once the Trust/Security layer
  lands in v0.3." Smallest change, smallest risk.
- **Option B — both.** Keep `pip install mnemlet` as the preferred line
  but add a note that v0.2.0 is git-only until PyPI lands. Riskier —
  first-time visitors copy the first command and get 0.1.0 (or nothing)
  silently.
- **Option C — block on PyPI publish.** Stop this plan, write a
  separate PyPI-publish plan, run it, then come back. Most work but
  honours the current README claim.

Implementer must record Christoph's choice at the top of Task 1 before
editing.

**Recorded decision: Option A — chosen by Christoph on 2026-05-24.**
Quickstart will switch to
`pip install git+https://github.com/christoph/mnemlet.git@v0.2.0`,
with one sentence noting that PyPI follows once the Trust/Security
layer lands in v0.3.

---

## File Structure

### Existing files to modify

- `README.md` — MCP tool count, tool list, API endpoint table,
  Quickstart command, "Tests" badge wording if it claims a count that
  diverges from current `190 passed`.
- `AGENTS.md` — phase label, architecture summary, anything that still
  reads v0.1.

### New files to create

- None.

---

## Task 1: README on v0.2 truth

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Record Quickstart decision**

At the top of the implementer's task notes, write "Quickstart option:
A | B | C — chosen by Christoph on 2026-05-24". Do not start editing
without a recorded decision.

- [ ] **Step 2: Verify the MCP tool list against the code**

Run:

```bash
grep -A1 -nE '@mcp\.tool' src/mnemlet/server/mcp_server.py | grep -E 'def ' | sed -E 's/.*def +([a-z_]+).*/\1/'
```

Expected: 14 names matching the list in this plan. If the count or any
name differs, stop and update the plan before editing the README.

- [ ] **Step 3: Verify the REST endpoint list against the code**

For each file in `src/mnemlet/server/routes/*.py`, read the file and
note:

- the `APIRouter(prefix=...)` value (if any),
- every `@router.(get|post|put|delete|patch)("/...")` path.

Combine prefix + path to get the canonical URL. Use these canonical
URLs in the README, not the relative paths from this plan.

- [ ] **Step 4: Update the README "What Mnémlet Does" bullet**

In `README.md`, replace:

> 🔌 **MCP-native with 8 tools** — `mnemlet_ingest`, `mnemlet_recall`,
> `mnemlet_search`, `mnemlet_status`, `mnemlet_namespaces`,
> `mnemlet_update`, `mnemlet_decay_config`, `mnemlet_export`. …

with a v0.2-correct version that names 14 tools and groups them
(ingest/recall/search · context+explain · review · status/admin).
Keep the OpenWebUI / OpenClaw / Claude Code / Cursor / MCP client
sentence intact.

- [ ] **Step 5: Update the README API Reference table**

In the section starting `## API Reference`, replace the table body with
the canonical URLs collected in Step 3. Include the six v0.2 endpoints
(`/remember`, `/forget/{id}`, `/replace/{id}`, `/confirm/{id}`,
`/explain/{id}`, `/context`). Remove any row whose path no longer
exists in the code (e.g. `/sleep/status` if Step 3 confirms it is
gone). Keep `/mcp` SSE.

- [ ] **Step 6: Update the README Quickstart per the recorded decision**

If Option A: replace `pip install mnemlet` block with the
`pip install git+…@v0.2.0` form and add one sentence about PyPI
following v0.3. If Option B: keep `pip install mnemlet` and add a note
that v0.2.0 is git-only for now. If Option C: stop the plan.

- [ ] **Step 7: Sanity-read the README end to end**

Read the whole `README.md` top to bottom. Flag any remaining v0.1-only
claim (e.g. test count badge, comparison table tool count cell). Fix
each one or note it as out of P0 scope.

- [ ] **Step 8: Verify the README renders sane**

```bash
.venv/bin/pytest -q
```

Tests should still pass; no source files were touched.

- [ ] **Step 9: Stop and report**

Report to Christoph: what changed (bullet list of edits per README
section), what was deferred, evidence (`git diff README.md` summary).
Do not commit yet — wait for go-ahead.

---

## Task 2: AGENTS.md on v0.2 truth

**Files:**
- Modify: `AGENTS.md`

- [ ] **Step 1: Read the current file**

Read `AGENTS.md` in full. Note every line that names a phase, a feature
set, or an architecture component.

- [ ] **Step 2: Replace the phase label**

Current line: `Phase: 1 — Core Engine`.
Replace with: `Phase: 2 — Memory Intelligence + Benchmark Suite (v0.2.0
released 2026-05-24, v0.3 = Trust/Security/Privacy)`.

- [ ] **Step 3: Refresh the architecture summary**

Update the `**Architecture:**` block so it reflects current modules.
At minimum:

- `storage/` — SQLite/FTS5, ChromaDB, ONNX embeddings, Markdown vault
- `engine/` — ingest, recall, decay, sleep engine
- `intelligence/` — memory classifier, supersession, review service,
  context packs (only if the module exists; verify with `ls src/mnemlet`)
- `benchmark/` — datasets, runner, quality runner, adapters, reports
- `server/` — FastAPI routes, MCP server (14 tools)
- `client/` — Python SDK

Verify each named module against `ls src/mnemlet/` before claiming it.

- [ ] **Step 4: Update the "Key Rules" block only if it is wrong**

Read the rules. Do not rewrite them for style. Only fix outright
falsehoods (e.g. tool count, language version, test runner).

- [ ] **Step 5: Stop and report**

Report what changed in `AGENTS.md`. Do not commit yet.

---

## Task 3: Verification, commit, tag move

**Files:**
- None to modify in this task. This task only verifies and bundles.

- [ ] **Step 1: Run the full suite**

```bash
.venv/bin/pytest -q
```

Expected: `190 passed` (or higher), no failures. If failures appear,
stop — a doc-only change should not break tests; investigate.

- [ ] **Step 2: Inspect the staged surface**

```bash
git status --short
git diff README.md AGENTS.md
```

Expected: only `README.md` and `AGENTS.md` modified; no source or test
files in the diff.

- [ ] **Step 3: Commit**

```bash
git add README.md AGENTS.md
git commit -m "docs: align v0.2 surface with code"
```

Commit-message style: match existing convention (`docs:` prefix, single
line, no Co-Authored-By trailer — repo does not use one).

- [ ] **Step 4: Move the v0.2.0 tag to the new HEAD**

Because the v0.2.0 tag was created locally and never pushed, the
cleanest first public push is to have `v0.2.0` point at the polished
HEAD, not at the half-true release commit. Move the annotated tag:

```bash
git tag -d v0.2.0
git tag -a v0.2.0 -m "v0.2.0 — Memory Intelligence Core + Benchmark Suite + Quality Hardening"
git log --oneline -3
git tag
```

Expected: `v0.2.0` now points at the new docs-alignment commit; tag
list shows only `v0.2.0`.

- [ ] **Step 5: Report final state**

Report to Christoph:

- `git log --oneline -3`
- `git tag` with the new tag pointing at HEAD
- explicit confirmation that **no push** was performed
- the next-best follow-up (P1: CI, CONTRIBUTING, ROADMAP, demo refresh,
  website refresh; or P2; or PyPI publish plan).

---

## Out of Scope for this Plan

- PyPI publish (separate plan, includes credential check, build, dry-run
  install, optional test PyPI, publish, post-publish smoke test).
- `.github/` setup (CI, issue templates, PR template) — P1.
- `CONTRIBUTING.md`, `ROADMAP.md`, `CODE_OF_CONDUCT.md` — P1/P2.
- `website/` refresh — P1.
- `scripts/demo.cast` re-recording — P1.
- Any source-code change. If a doc claim cannot be made true without
  changing source, that claim is removed in P0 and the source change
  goes into its own plan.

---

## Execution Notes

- Implementer pauses between tasks. Christoph confirms before the next
  task begins. This is a usage-budget guard, not a process formality.
- All edits are reversible via `git checkout README.md AGENTS.md` until
  Step 3 of Task 3 commits.
- The tag-move in Task 3 Step 4 is safe only because nothing was ever
  pushed. If `origin/v0.2.0` exists at the start of Task 3, stop and
  ask before proceeding.
