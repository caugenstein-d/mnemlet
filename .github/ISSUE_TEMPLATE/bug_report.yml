name: Bug report
description: Something is wrong with Mnémlet.
labels: ["bug"]
body:
  - type: textarea
    id: what-happened
    attributes:
      label: What happened?
      description: A clear, short description of the bug.
    validations:
      required: true
  - type: textarea
    id: reproduce
    attributes:
      label: How to reproduce
      description: Commands, requests, or configuration that triggers the bug.
      render: bash
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: Mnémlet version
      description: Output of `pip show mnemlet` or the git commit SHA.
    validations:
      required: true
  - type: input
    id: env
    attributes:
      label: Environment
      description: OS, Python version, hardware (e.g. Raspberry Pi 5 16 GB).
    validations:
      required: true
  - type: textarea
    id: logs
    attributes:
      label: Logs or stack trace
      render: text
